Tengo un CSV con las siguientes columnas: [LISTAR]. Quiero que hagas:

1. Resumen estadístico: count, mean, std, min, max, percentiles 25/50/75 para columnas numéricas. Top 5 valores más frecuentes para categóricas.

2. Detecta outliers usando IQR (1.5 * IQR rule). Lista los 5 más extremos y por qué son outliers.

3. Correlación entre las columnas numéricas. Destaca las correlaciones fuertes (>0.7 o <-0.7) y sugiere una interpretación.

4. 3 gráficos que me ayudarían a entender los datos. Genera el código Python (pandas + matplotlib) para cada uno.
