Tengo un CSV con las siguientes columnas: [LISTAR]. Quiero que hagas:

1. Resumen estadistico: count, mean, std, min, max, percentiles 25/50/75 para columnas numericas. Top 5 valores mas frecuentes para categoricas.

2. Detecta outliers usando IQR (1.5 * IQR rule). Lista los 5 mas extremos y por que son outliers.

3. Correlacion entre las columnas numericas. Destaca las correlaciones fuertes (>0.7 o <-0.7) y sugiere interpretacion.

4. 3 graficos que me ayudarian a entender los datos. Genera el codigo Python (pandas + matplotlib) para cada uno.
